<h1>Update Pet</h1>
<form action="/pets/<%= @pet.id%>" method="POST">
  <label>Pet Name:</label>

  <br></br>

  <input type="text" id="pet_name" name="pet_name" value="<%= @pet.name %>">

  <br><br>

  <label>Choose an existing owner:</label>

  <br><br>

  <% @owners.each do |owner| %>
    <input type="radio" name="owner_id" value="<%= owner.id %>" id="<%= owner.id %>" <%= 'checked' if @pet.owner_id == owner.id %> >
      <%= owner.name %>
    </input>
  <% end %>

  <br></br>

  and/or, create a new owner:

  <br><br>

  <label>name:</label>
  <input type="text" id="owner_name" name="owner[name]"></input>

  <br><br>

  <input type="submit" value="Update Pet">
</form>
